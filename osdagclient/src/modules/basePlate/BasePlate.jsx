import React from "react";

// Placeholder page for Base Plate until full UI is implemented.
// Keeps the route working so navigation does not break.
const BasePlate = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-3">Base Plate</h1>
      <p className="text-gray-700">
        The Base Plate module is not yet implemented in the web UI. Please check
        back once the backend endpoints are wired and UI is completed.
      </p>
    </div>
  );
};

export default BasePlate;

