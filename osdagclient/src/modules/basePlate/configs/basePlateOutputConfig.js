export const basePlateOutputConfig = {
  // Minimal output config for Base Plate module.
  // Add sections/fields here as needed to format the module output in the UI.
  sections: [
    {
      title: "Base Plate Results",
      fields: [
        // Example field mapping; frontend will render available outputs dynamically.
      ],
    },
  ],
};

export default basePlateOutputConfig;
